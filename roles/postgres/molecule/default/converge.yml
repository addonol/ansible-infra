---
- name: Converge Postgres Role
  hosts: all:!localhost
  gather_facts: false
  vars_files:
    - test_vars.yml

  tasks:
    - name: "Include postgres role"
      ansible.builtin.include_role:
        name: postgres
