---
vault_postgres_password: "testpassword123"
postgres_secret_db_password_name: "postgres_db_password_{{ inventory_hostname }}"
postgres_db_name: "postgres_db"
postgres_db_user: "postgres_user"
postgres_container_name: "postgres_molecule_{{ inventory_hostname }}"
postgres_network: "postgres_net_{{ inventory_hostname }}"
postgres_port_number: "{{ 5432 + (ansible_play_hosts.index(inventory_hostname)) }}"
postgres_volume_name: "postgres_data_{{ inventory_hostname }}"
