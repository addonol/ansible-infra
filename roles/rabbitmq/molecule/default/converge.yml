---
- name: Converge RabbitMQ broker Role
  hosts: all:!localhost
  gather_facts: false
  vars_files:
    - test_vars.yml

  tasks:
    - name: "Include RabbitMQ broker role"
      ansible.builtin.include_role:
        name: rabbitmq
